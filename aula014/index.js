/**
 * Não pode criar constantes com palavras reservadas
 * Constantes precisam ter nomes significativos 
 * Não pode começar o nome de uma constante com um número
 * Não pode conter espaços ou traços
 * Utilizamos camelCase
 * Case-sensitive, maiúscula é diferente de minúscula
 * Não pode modificar o valor de uma constante
 * Não utilizar 'var', utilizar sempre 'const'
 */

const nome = 'Alessandro'; // O valor atribuido aqui não pode ser alterado no decorrer do código
console.log(nome);
console.log(typeof nome);